import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.plantdoctorai',
  appName: 'Plant Doctor AI',
  webDir: 'dist'
};

export default config;
